@model IEnumerable<WebBookProject.Models.Publisher>

@{
    ViewData["Title"] = "Yayınevi";
    Layout = "~/Views/Shared/_SidebarLayout.cshtml";

}

<h1>Yayınevleri</h1>

<p>
    <a class="btn btn-primary" style="color:white" asp-area="Admin" asp-controller="Publishers" asp-action="Create">Yayınevi Ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Yayınevi Adı
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PublisherName)
                </td>
                <td>
                    <a class="btn btn-success" style="color:white" asp-action="Edit" asp-route-id="@item.PublisherId">Düzenle</a>
                </td>
                <td>
                    <a class="btn btn-dark" style="color:white" asp-action="Details" asp-route-id="@item.PublisherId">Detaylar</a>
                </td>
                <td>
                    <a class="btn btn-danger" style="color:white" asp-action="Delete" asp-route-id="@item.PublisherId">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
